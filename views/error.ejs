<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Oops! Page Not Found</title>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap');

    body, html {
        margin: 0;
        padding: 0;
        height: 100%;
        font-family: 'Inter', sans-serif;
        background: #0f111a;
        color: #ffffff;
        display: flex;
        justify-content: center;
        align-items: center;
        overflow: hidden;
    }

    .container {
        text-align: center;
        z-index: 2;
        padding: 40px;
        background: rgba(20, 20, 35, 0.85);
        border-radius: 15px;
        box-shadow: 0 0 40px rgba(0,0,0,0.5);
        animation: float 3s ease-in-out infinite;
    }

    h1 {
        font-size: 100px;
        color: #e63946;
        margin-bottom: 20px;
        animation: bounce 2s infinite;
    }

    h2 {
        font-size: 28px;
        margin-bottom: 15px;
    }

    p {
        color: #a0aec0;
        font-size: 16px;
        margin-bottom: 30px;
    }

    a {
        text-decoration: none;
        color: #ffffff;
        background: linear-gradient(90deg, #6a11cb 0%, #2575fc 100%);
        padding: 12px 30px;
        border-radius: 8px;
        font-weight: 600;
        transition: transform 0.3s, box-shadow 0.3s;
    }

    a:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 25px rgba(0,123,255,0.5);
    }

    /* Floating animation */
    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }

    @keyframes bounce {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-20px); }
    }

    /* Background animated circles */
    .background-circles {
        position: absolute;
        width: 100%;
        height: 100%;
        overflow: hidden;
        top: 0;
        left: 0;
        z-index: 1;
    }

    .circle {
        position: absolute;
        border-radius: 50%;
        opacity: 0.2;
        animation: move 20s linear infinite;
    }

    @keyframes move {
        0% { transform: translateY(100vh) scale(0.5); }
        100% { transform: translateY(-100vh) scale(1); }
    }

</style>
</head>
<body>

<div class="background-circles">
    <div class="circle" style="width:100px;height:100px;left:10%;background:#6a11cb;animation-delay:0s;"></div>
    <div class="circle" style="width:150px;height:150px;left:40%;background:#2575fc;animation-delay:5s;"></div>
    <div class="circle" style="width:80px;height:80px;left:70%;background:#e63946;animation-delay:10s;"></div>
</div>

<div class="container">
    <h1>404</h1>
    <h2>Page Not Found</h2>
    <p>Sorry, we couldn't find the page you were looking for.</p>
    <a href="/">Go Back Home</a>
</div>

</body>
</html>
